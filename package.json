{
  "name": "erp-project",
  "version": "1.0.0",
  "description": "ERP system with React frontend and Express backend",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm --prefix backend dev\" \"npm --prefix frontend dev\"",
    "build": "npm run build --prefix frontend && npm run build --prefix backend",
    "start": "npm --prefix backend start"
  },
  "workspaces": [
    "backend",
    "frontend"
  ],
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
